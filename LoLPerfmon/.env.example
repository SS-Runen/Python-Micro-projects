# Copy to ".env" in this folder or repo root (see .gitignore — never commit .env).
#
# Riot Games API (developer.riotgames.com) — only if you call *.api.riotgames.com
# (summoner, match history, etc.). LoLPerfmon simulation does NOT use this.
#
# Data Dragon (https://ddragon.leagueoflegends.com) — static JSON/images, no key.
# See: https://developer.riotgames.com/docs/lol#data-dragon
#
# RIOT_API_KEY=
